void _core() {
  
  // Verificando se o Acumulador atingiu o valor de credito
  checkAcc();

  // Verificando se tem que atualiza os dados de captura
  captura_check();
  //noteiro_check();
  //moedeiro_check();
  
  delay(10);
}
